# C语言训练平台 - 详细需求文档

## 项目概述

**项目名称**: C语言硬核代码思维训练器  
**目标用户**: 嵌入式/Linux系统编程学习者、面试准备者  
**核心价值**: 通过交互式练习和可视化，深入理解C语言底层原理

---

## 功能模块

### 1. 章节练习系统

#### 1.1 章节结构
| 章节ID | 名称 | 图标 | 题目数量 | 难度 |
|--------|------|------|----------|------|
| basics | C语言基础 | 📚 | 46 | 1-2 |
| interview | 面试专题 | 💼 | 30 | 3-5 |
| linux | Linux系统编程 | 🐧 | 30 | 4-5 |

#### 1.2 题目类型
- **填空题 (fill)**: 补全代码中的空缺
- **输出预测 (output)**: 预测程序输出结果
- **Bug修复 (debug)**: 找出并修复代码错误
- **代码排序 (order)**: 按正确顺序排列代码行
- **多空填空 (multi-fill)**: 多个独立填空

#### 1.3 题目数据结构
```typescript
interface Question {
  id: number;                    // 全局唯一ID
  chapter: string;               // 所属章节
  type: 'fill' | 'output' | 'debug' | 'order' | 'multi-fill';
  title: string;                 // 题目标题
  description?: string;          // 题目描述
  code?: string;                 // 代码内容
  blanks?: {                     // 填空配置
    hint: string;               // 提示
    answer: string | string[];  // 答案（支持多答案）
    caseSensitive?: boolean;    // 是否区分大小写
  }[];
  correctOutput?: string;        // 正确输出（output类型）
  bugLine?: number;              // Bug所在行（debug类型）
  bugFix?: string;               // 修复后的代码
  codeLines?: string[];          // 代码行（order类型）
  correctOrder?: number[];       // 正确顺序（order类型）
  explanation: string;           // 详细解析
  difficulty: 1 | 2 | 3 | 4 | 5; // 难度等级
  vocabulary?: {                 // 英文词汇教学
    word: string;
    meaning: string;
  }[];
}
```

---

### 2. 项目实战系统

#### 2.1 项目结构
```typescript
interface Project {
  id: string;
  name: string;
  description: string;
  difficulty: 'intermediate' | 'advanced' | 'expert';
  totalLines: number;
  fragments: ProjectFragment[];
}

interface ProjectFragment {
  id: number;
  title: string;
  code: string;
  explanation: string;
  keyPoints: string[];
  interviewTips: string[];
  visualization?: {
    type: 'flow' | 'memory' | 'architecture' | 'output';
    data: any;
  };
  practiceQuestions: {
    question: string;
    answer: string;
  }[];
}
```

#### 2.2 当前项目
**HTTP服务器 + 日志系统** (8个片段)
1. 项目概述与架构设计
2. Socket创建与绑定
3. 多线程并发处理
4. HTTP请求解析
5. 静态文件响应
6. 日志系统设计
7. 信号处理与优雅退出
8. 性能优化与完整代码

---

### 3. 互动实验室

#### 3.1 实验室组件列表

| 组件名 | 功能 | 教学价值 |
|--------|------|----------|
| PointerSandbox | 指针操作沙盒 | 理解指针、内存、错误检测 |
| StackFrameViz | 函数栈帧可视化 | 理解函数调用、递归 |
| COWMemoryViz | COW机制可视化 | 理解fork写时复制 |
| BitSwitchGame | 位运算开关箱 | 理解二进制/十六进制 |
| StructPackerGame | 结构体内存布局 | 理解内存对齐 |

#### 3.2 PointerSandbox 详细规格

**布局**: 三栏布局
- 左侧栏 (25%): 示例代码列表、步骤说明
- 中间栏 (42%): 代码编辑器、内存可视化
- 右侧栏 (33%): 控制台、变量监视、调用栈

**支持的C语法**:
```c
int a = 100;                    // 栈变量声明
int *p = &a;                    // 指针声明
int arr[3] = {1, 2, 3};         // 数组声明
*p = 200;                       // 解引用赋值
printf("%d", *p);               // 打印指针值
printf("%d", arr[0]);           // 打印数组元素
p = malloc(4);                  // 堆分配
free(p);                        // 释放内存
```

**错误检测**:
- 野指针 (使用未初始化指针)
- Use-After-Free (访问已释放内存)
- 数组越界
- 双重释放
- 内存泄漏 (警告)

**调试功能**:
- 断点设置 (点击行号)
- 单步执行 (上一步/下一步)
- 自动运行 (支持断点暂停)
- 步骤进度条

**内存可视化**:
- 栈区: 蓝色，显示局部变量
- 堆区: 紫色，显示malloc分配的内存
- 已释放: 红色标记
- 指针: 琥珀色，显示指向地址

---

### 4. 用户界面

#### 4.1 导航结构
```
┌─────────────────────────────────────────────────────┐
│  Logo    [首页] [练习] [项目实战] [实验室] [统计]    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  [内容区域]                                          │
│                                                     │
└─────────────────────────────────────────────────────┘
```

#### 4.2 主题支持
- 深色模式 (默认)
- 浅色模式
- 自动保存用户偏好

#### 4.3 进度追踪
- 本地存储 (localStorage)
- 已完成题目记录
- 正确/错误统计
- 尝试次数记录

---

### 5. 技术栈

| 层级 | 技术 |
|------|------|
| 框架 | React 18 + TypeScript |
| 构建 | Vite 7 |
| 样式 | Tailwind CSS 4 |
| 图标 | Lucide React |
| 部署 | 单文件HTML (vite-plugin-singlefile) |

---

### 6. 数据结构总览

#### 6.1 文件结构
```
src/
├── App.tsx                    # 主应用组件
├── index.css                  # 全局样式
├── data/
│   ├── lessons.ts            # 章节和题目数据
│   ├── projects.ts           # 项目数据
│   ├── vizData.ts            # 可视化数据映射
│   └── vocabulary.ts         # 词汇处理
└── components/
    ├── PointerSandbox.tsx    # 指针沙盒
    ├── StackFrameViz.tsx     # 栈帧可视化
    ├── COWMemoryViz.tsx      # COW可视化
    ├── BitSwitchGame.tsx     # 位运算游戏
    ├── StructPackerGame.tsx  # 结构体游戏
    ├── ProjectView.tsx       # 项目视图
    ├── Visualizations.tsx    # 可视化组件
    ├── SyntaxHighlighter.tsx # 语法高亮
    └── ReferenceSidebar.tsx  # 参考侧边栏
```

#### 6.2 数据规模
- **题目总数**: 106道
  - 基础题: 46道
  - 面试题: 30道
  - Linux题: 30道
- **项目片段**: 8个
- **实验室组件**: 5个

---

### 7. 特色功能

#### 7.1 智能输入处理
- 中文符号自动转换 (≠ → !=, ≤ → <=)
- Tab键支持 (在output类型题目中)
- 空答案提交支持

#### 7.2 可视化系统
- 内存布局可视化
- 指针指向关系箭头
- 栈帧增长动画
- COW过程动画

#### 7.3 教学辅助
- 英文词汇提示
- 详细解析说明
- 难度分级
- 面试要点总结

---

### 8. 性能要求

- **首屏加载**: < 2秒
- **交互响应**: < 100ms
- **动画帧率**: 60fps
- **构建产物**: < 200KB (gzip)

---

### 9. 浏览器兼容性

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

### 10. 未来扩展计划

1. **更多项目实战**
   - 内存池实现
   - 环形缓冲区
   - 状态机解析器

2. **增强实验室**
   - 汇编代码查看
   - 性能分析器
   - 多线程调试器

3. **社交功能**
   - 用户账号
   - 进度同步
   - 排行榜

4. **移动端适配**
   - 响应式布局优化
   - 触摸交互支持

---

## 附录

### A. 题目难度定义
| 等级 | 描述 |
|------|------|
| 1 | 基础语法，直接记忆 |
| 2 | 简单逻辑，单步推理 |
| 3 | 综合应用，多步推理 |
| 4 | 深度理解，底层原理 |
| 5 | 专家级，复杂场景 |

### B. 代码风格规范
- 使用4空格缩进
- if/for/while后必须加花括号
- 变量名使用小写+下划线
- 指针声明: `int *p` (星号靠近变量名)
- 函数注释说明参数和返回值

### C. 错误码定义
| 错误类型 | 说明 |
|----------|------|
| E001 | 野指针访问 |
| E002 | Use-After-Free |
| E003 | 数组越界 |
| E004 | 双重释放 |
| E005 | 内存泄漏 |
| E006 | 栈溢出 |

---

**文档版本**: v1.0  
**最后更新**: 2026-02-11  
**作者**: Claude Code
