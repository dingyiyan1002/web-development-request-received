// 可视化数据映射 - 按题目ID对应
// 注意：只添加真正有助于理解的可视化，不要为了可视化而可视化
import type { VizData } from '../components/Visualizations';

export const vizMap: Record<number, VizData> = {
  // 暂时清空，需要时再添加真正有用的可视化
  // 只保留复杂算法和指针操作的可视化
};
